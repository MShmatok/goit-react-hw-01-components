import { styled } from 'styled-components';

export const Container = styled.ul`
  list-style: none;
  width: 300px;

  margin-left: 5px;

  display: flex;
  flex-direction: column;

  gap: 5px;
`;
